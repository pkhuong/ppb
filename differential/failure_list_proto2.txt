# Conformance failures the PPB testee is expected to produce, for
# protobuf_test_messages.proto2.TestAllTypesProto2 under PROTOBUF output.
# Every entry is explained by a documented gap in GAPS.md; the
# tags below map to the "Residual divergences" section there.
#
# Regenerate/triage: `make -C differential protobuf-prefix` once (builds the
# pinned protobuf + conformance_test_runner into a sibling ../protobuf/_install),
# then `make -C differential conformance-proto2`; or `make -C differential
# conformance-docker` to run the whole suite in the image with no host checkout
# at all. A `CONFORMANCE_RUNNER=<path>` override still works against your own
# runner.
#
# All unlisted failures are unexpected, but could be triggered by new test cases.

# --- (groups) proto2 group wire type not supported ---
# PPB does not support the proto2 group wire type (SGROUP/EGROUP); a payload
# containing a group tag is rejected with a parse error.  ValidMessageSetEncoding
# uses the MessageSet wire format which encodes items as groups.
# MessageSetEncoding.UnknownExtension is an extension-field group that PPB
# likewise rejects.
Required.Proto2.ProtobufInput.ValidMessageSetEncoding.ProtobufOutput
Required.Proto2.ProtobufInput.ValidMessageSetEncoding.OutOfOrderGroupsEntries.ProtobufOutput
Required.Proto2.ProtobufInput.MessageSetEncoding.UnknownExtension.ProtobufOutput
